// Debug strategy emits a fixed intent sequence, so exit logic is intentionally minimal.
// The file exists to satisfy the shared strategy structure contracts.
export const evaluateDebugExit = (): null => null;
