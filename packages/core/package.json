{
  "name": "@agenai/core",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "dotenv": "^16.4.5"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node src/index.ts",
    "lint": "eslint 'src/**/*.{ts,tsx}' --max-warnings=0 || true",
    "test": "vitest run"
  }
}
