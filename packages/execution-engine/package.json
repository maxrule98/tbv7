{
  "name": "@agenai/execution-engine",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "dependencies": {
    "@agenai/core": "workspace:*",
    "@agenai/exchange-binance": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "ts-node src/index.ts",
    "lint": "eslint 'src/**/*.{ts,tsx}' --max-warnings=0 || true",
    "test": "vitest run"
  }
}
